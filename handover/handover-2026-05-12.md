# HANDOVER - 2026-05-12

## What We Were Doing (1-3文)

OpenAI が 2026 年に発表した `gpt-realtime-translate` モデルを `realtime-caption` プロジェクトに統合する大型作業。
8 時間連続稼働モードで PR #7（gpt-realtime-translate 取り込み）から始まり、API キー難読化・gitleaks 自動化・VB-CABLE Zoom 同時通訳・コスト保護・FAQ・QA Suggestion クリーンアップまで 7 PR を完遂、すべて master にマージ済み。途中で DeepL API キー漏洩事案が発生し、git filter-branch + force push で履歴クリーン化、再発防止策を dotfiles の全エージェント定義に永続化。

## Current State (最重要！)

- Branch: `master`（clean、push 済み）
- Working / Functional:
  - `gpt-realtime-translate` 統合（WebSocket、PCM16 音声送受信、句読点フォールバック確定、指数バックオフ再接続）
  - API キー base64 難読化 + GUI 入力 UI（後方互換あり）
  - gitleaks pre-commit + GitHub Actions CI（DeepL/OpenAI/AWS/Slack/GitHub PAT 等を検出）
  - VB-CABLE 仮想マイク出力（Zoom 同時通訳デバイス化）
  - コスト保護（max_session_minutes、$5/$10/$20 警告閾値、GUI に経過/想定コスト表示）
  - PyInstaller spec 更新（realtime_translator / audio_output / websockets.asyncio）
  - FAQ.md（コスト・帯域・レイテンシの目安、英日両方）
  - CONTRIBUTING.md（テスト fixture 命名規約）
  - atomic write 化、テストヘルパー共通化、_find_zoom_preset_output pure function 切り出し
- Broken / Incomplete / Untested:
  - **PR #7 の実機 E2E 未実施**: 監督が config を切り替えて起動テスト未完了
  - **PR #13 の実機 E2E 未実施**: VB-CABLE インストール + Zoom 通話確認が必要
  - **PR #16 のコスト保護実機 E2E 未実施**: max_session_minutes 到達 → shutdown 動作
  - **session.input_transcript.delta 取りこぼし発覚**（Issue #23）: 公式ドキュメント確認で原文取得可能と判明、PR #7 実装が SPECちゃん類推で漏らした
  - **デバイス重複表示問題**（Issue #21）: pyaudio が Host API ごとに同一物理デバイスを列挙
  - **詳細設定の動的表示制御未実装**（Issue #20）
- Build / Test / Run status:
  - 全テスト 80+ 件 PASS（`pytest tests/ -v`）
  - master のテスト全 PASS
  - GUI 実機起動は未確認（Bash agent 環境では DearPyGui 起動不可）
- Recent commits (最新3件):
  - `bf9d1cd` chore: address QA suggestions cleanup (closes #15) (#19)
  - `4e9c072` feat(zoom): VB-CABLE virtual mic output for simultaneous interpretation (closes #9) (#13)
  - `e194798` feat(cost): max session timer + cost display + warning thresholds (closes #14) (#16)

## Key Decisions & Rationale

- **base64 難読化を選択（暗号化ではない）** → 監督指示。実装シンプル、配布先で追加セットアップ不要。スクショ・casual 閲覧から守れれば十分というユースケース。本格的な機密性保護（DPAPI/Fernet/Keychain）は将来別 issue 化
- **session 制御に `session.` プレフィックス必須** → 実機エラーから判明。標準 Realtime API（プレフィックス無し）と translation エンドポイント（プレフィックス必須）で仕様が異なる
- **PR #16/#13 は PR #7 base** → 機能依存関係上、PR #7 のマージ後に master へ rebase。マージ順は dotfiles → 独立 PR → PR #7 → PR #16/#13 の順
- **入力デバイス制限を撤廃** → VB-CABLE ユースケースでマイク入力が必須。元々の loopback 限定は安全策だったが本機能で解禁
- **PyAudio インスタンスを AudioOutputStream が所有** → CaptionSystem に責務を持たせると shutdown 順序が複雑になる
- **gitleaks の汎用 -fake- インフィックスを allowlist 拒否** → 攻撃者が `sk-realKey-test-...` で検出回避を狙うのを防ぐため。具体的フォーマットのみ許可
- **DeepL リーク後の filter-branch 採用** → git-filter-repo 未インストール、filter-branch + `--tree-filter` で sed 置換、`refs/original/` 削除、`reflog expire` + `gc --prune=now --aggressive`、force-with-lease で push
- **再発防止策を dotfiles に永続化** → SPEC/PRG/QA の各 agents/*.md に役割別ルール。SPEC で実 API キー引用禁止、PRG で SPEC ミス連鎖防止、QA で Critical「秘密情報パターン検出」必須化

## Files Changed This Session (影響度順)

本セッションの変更は全て master に squash merge 済み。master の最新コミットを参照:

- `0d0c358` PR #7: `realtime_translator.py`(新), `main.py`, `app.py`, `config.yaml.example`, `requirements.txt`, `download_models.py`, `realtime-caption.spec`, `tests/test_realtime_translator.py`(新)
- `a623876` PR #10: `config_utils.py`(新), `app.py`, `main.py`, `config.yaml.example`, `README.md`, `tests/test_config_decoder.py`(新), `tests/test_save_api_keys.py`(新), `tests/__init__.py`(新)
- `a7022f2` PR #12: `.pre-commit-config.yaml`(新), `.gitleaks.toml`(新), `.github/workflows/gitleaks.yml`(新), `CONTRIBUTING.md`(新), `README.md`
- `bbcd13f` PR #18: `FAQ.md`(新), `README.md`
- `4e9c072` PR #13: `audio_output.py`(新), `realtime_translator.py`, `main.py`, `app.py`, `config.yaml.example`, `README.md`, `realtime-caption.spec`, `tests/test_audio_output.py`(新), `tests/test_main_devices.py`(新), `tests/conftest.py`(新)
- `e194798` PR #16: `cost_monitor.py`(新), `main.py`, `app.py`, `config.yaml.example`, `README.md`, `conftest.py`(新), `tests/test_cost_monitor.py`(新)
- `bf9d1cd` PR #19: `app.py`, `audio_output.py`, `realtime_translator.py`, `config.yaml.example`, `.gitleaks.toml`, `tests/test_save_api_keys.py`, `tests/test_app_gui.py`(新)

dotfiles リポジトリ（PR #51 マージ済み）:
- `claude/CLAUDE.md` セキュリティセクション拡張
- `claude/agents/{spec,prg-chan,qa-agent}.md` 再発防止ルール追加

## Blockers, Gotchas & Workarounds

- **DeepL API キー public push 漏洩**（2026-05-12）
  - 原因: 前セッションで Read ツールが config.yaml の中身を会話に出力 → SPECちゃんが仕様書サンプルに転記 → PRG ちゃんがテストに転記 → push 直後に GitGuardian 検出
  - 解決: 監督がキー無効化、`git filter-branch --tree-filter` で全履歴を sed 置換、force-push で origin 上書き
  - 再発防止: CLAUDE.md + 全エージェント定義に「テスト・サンプル値・ドキュメントでの秘密情報禁止」ルール永続化、PR #12 で gitleaks 自動化

- **OpenAI キーも部分露出**（QA レビュー中）
  - 原因: QA ちゃんが config.yaml を Read して OpenAI キーの頭尾（プレフィックス数文字 + magic string）を会話に引用
  - 監督対応: Usage Dashboard 確認、異常なし → 「後で revoke」判断
  - 今後の対応: 「config.yaml を Read しない」ルールを全エージェントに徹底

- **`session.input_audio_buffer.append` の prefix 必須**
  - 実機エラー `Invalid value: 'inp...end'. Supported values are: 'session.update', 'session.input_audio_buffer.append', and 'session.close'.` から判明
  - 解決: 全送信 type に `session.` プレフィックス追加
  - 再発防止: API 仕様未確認部分は verbose ログ（`RT_RAW_UNKNOWN`）で全イベント記録

- **PR conflict 連鎖**
  - PR #10/#12/#18 マージ後に PR #7 で app.py conflict、PR #7 マージ後に PR #16/#13 で多数 conflict、PR #16 マージ後に PR #13 で再 conflict
  - 解決: PRG ちゃんに委譲、両側の機能を活かして手動マージ、テスト全 PASS 確認後 push

- **session.input_transcript.delta 取りこぼし**（Issue #23 として記録）
  - 原因: SPECちゃんが「API は原文を返さない」と類推 → PR #7 実装で受信処理を入れず
  - 真因: 公式ドキュメント確認で原文も返ることが判明、SPECちゃんの WebFetch が当時 402 で失敗していた
  - 解決方針: Issue #23 で `RealtimeTranslator.on_source_transcript` コールバック追加（次セッションで対応）

- **デバイス重複表示**（Issue #21）
  - 原因: pyaudio が Host API（MME / DirectSound / WASAPI / Loopback）ごとに同一物理デバイスを別エントリで列挙
  - 解決方針: WASAPI フィルタ採用 + 必要なら Host API 名をデバイス名に付記

## Key Learnings & Gotchas (長期記憶へ移行推奨)

- **Read ツールが credential ファイルを開くと値が会話全体に流れる**: Anthropic 内部 session のみだが、SPEC → PRG への転記で外部漏洩に繋がる連鎖を生む
- **TDD の RED コミットは git log で検証可能**: QA レビューで「テストファースト遵守」を機械的にチェックできる
- **API 仕様の類推実装は危険**: 公式ドキュメントが取得不可（402 等）でも、verbose ログで全イベントを保守的に記録すれば後で真実が判明する。Issue #23 がその一例
- **filter-branch は遅いが確実**: git-filter-repo が無くても代替可能。tree-filter + sed で履歴全体を書き換え
- **squash merge で base ブランチが消えた後の PR**: base を master に切り替え + merge コミットで master 側の進捗を取り込めば conflict 解消可能。GitHub の "Change base branch" を `gh pr edit --base master` で実施
- **dotfiles の symlink 同期は方向性がファイルごとに違う**: agents/* は symlink で双方向同期、CLAUDE.md は別ファイル（dotfiles 側のサイズと .claude 側のサイズが違う）→ 両方を手動同期する必要あり
- **gitleaks の汎用パターン allowlist は脆弱性**: `-fake-` / `-test-` などの汎用インフィックスを allowlist にすると、攻撃者が `sk-realKey-test-...` で回避を狙える。具体フォーマットのみ許可すべき
- **PyAudio の Host API**: Windows では同一物理デバイスが 3-4 個 (MME/DirectSound/WASAPI/Loopback) 列挙される。WASAPI 優先 + Loopback 別表記が現代的

## Next Steps (優先度順・具体的！)

### High (今日〜明日)
- **Issue #23 (input_transcript 取りこぼし修正)**: `RealtimeTranslator.on_source_transcript` 追加 → overlay.html 上段に原文表示。次セッションで最優先対応推奨
- **Issue #21 (デバイス重複表示)**: WASAPI フィルタ実装。監督が今 VB-CABLE 動作確認中なのでハマっている問題
- **PR #7/#13/#16 の実機 E2E**: 監督側で VB-CABLE インストール後、config.yaml を `openai-realtime` に切替えて起動 → マイク発話 → Zoom に英語音声届くか確認
- **OpenAI API キー revoke + 新規発行**: 監督が「後で変える」とのこと、忘れずに

### Medium (今週)
- **Issue #20 (詳細設定の動的表示制御)**: 翻訳エンジン選択に応じて Whisper 関連 / Realtime 関連 UI を出し分け
- **Issue #22 (overlay Realtime 最適化)**: Issue #23 解決後に再評価。原文取得遅延時の見栄え対応
- **既存 Issue #1-#5 の整理**: 私のスコープ外、内容確認して優先度判断

### Low
- **強い暗号化への移行**（DPAPI / Fernet / Windows Credential Manager）: base64 難読化の上位対策、別 issue 化候補
- **OpenAI 旧形式キー gitleaks カスタムルール追加**: 現運用キーは v2 形式なので緊急性低
- **PyInstaller 実ビルド確認**: hiddenimports 追加後の動作確認、配布バイナリ生成

## Risks & Warnings

- **OpenAI API キーは漏洩済み**（前セッション Read 経由 + 今セッション QA レビュー部分露出）。監督が後で revoke 予定。それまで Usage Dashboard で異常監視推奨
- **gpt-realtime-translate API 仕様変動**: 2026 年リリース直後。verbose ログ `RT_RAW_UNKNOWN` でフォールバック既存
- **コスト保護未実装環境での長時間稼働**: max_session_minutes デフォルト 60 分だが、CLI モード起動時の確認プロンプトを無視するとリスク
- **VB-CABLE は Windows 限定**: Mac は BlackHole / Loopback、Linux は PulseAudio loopback
- **フィードバックループ防止は使用者責任**: 翻訳音声がマイクに戻ると無限ループ
- **次セッションで config.yaml を Read してはいけない**: 過去 2 回の漏洩事案、全エージェント定義に永続化済みだが要警戒

## Context Gaps

- **CLEAR**:
  - 7 PR の構成・依存関係・マージ順序
  - 80+ 件テスト全 PASS
  - リーク履歴削除完了（git grep で 0 件確認）
  - 監督の意思決定（OpenAI キー後で revoke、Phase ごとに承認、マージは手動指示）
  - Issue #23 の真因（公式ドキュメント確認結果）
- **FUZZY**:
  - PR #7/#13/#16 の実機 E2E 結果（監督側）
  - `session.input_transcript.delta` の正確なイベント形式（実装時に再確認必要）
  - デバイス重複の具体的な再現環境（監督 PC の Host API 構成）
- **GAPS / UNKNOWNS**:
  - VB-CABLE 実機動作（インストール環境必要）
  - 長時間稼働時のメモリ使用量・パフォーマンス
  - 配布バイナリビルド時の追加 hiddenimports 漏れの可能性

## Next Session Instructions

このHANDOVER.mdを最初に全文読み込み、状態・ブランチ・未完了タスク・Gotchasを完全に把握してから作業続行。矛盾・不明点は即質問。secretsは絶対出力しない。

**特に注意:**
- `config.yaml` を Read しないこと（過去 2 回事案：OpenAI 全文 + 部分露出、DeepL リーク）
- テスト fixture / サンプル値には `.gitleaks.toml` allowlist 登録済みのフェイク値のみ使用（`FAKE-TEST-KEY-...`, `sk-test-fake-...`, `your-api-key-here`）
- secrets を絶対に raw_log / handover / commit / memory に書かない（このファイルにも書いていない）
- 次セッションは Issue #23（input_transcript 取りこぼし修正）から最優先対応推奨

**監督の状況:**
- 「8時間連続稼働せよ」モードで Phase 1-7 完了 + Issue #20-23 追加起票
- OpenAI キーは「後で変える」
- VB-CABLE 動作確認中（Issue #21 のデバイス重複で苦戦の可能性）

raw_log: `../raw_logs/2026-05-12_session.md`
